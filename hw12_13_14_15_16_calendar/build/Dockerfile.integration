FROM golang:1.25

ENV CGO_ENABLED=0
ENV CODE_DIR /go/src/

WORKDIR ${CODE_DIR}

COPY go.mod go.sum ./
RUN go mod download

COPY . ${CODE_DIR}

CMD ["go", "test", "-v", "-count=1", "-timeout=120s", "./tests/integration/"]
