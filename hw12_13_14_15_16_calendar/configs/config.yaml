logger:
  level: "INFO"

server:
  host: "localhost"
  port: "8080"

database:
  dsn: "postgres://calendar:calendar@localhost:5432/calendar?sslmode=disable"

storage:
  type: "memory"  # "memory" или "sql"
