logger:
  level: "INFO"
  http_log_file: ""

server:
  host: "0.0.0.0"
  port: "8082"

database:
  dsn: "postgres://calendar:calendar@postgres:5432/calendar?sslmode=disable"

storage:
  type: "sql"
